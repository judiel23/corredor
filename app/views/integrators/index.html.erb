<p id="notice"><%= notice %></p>

<h1>Listing Integrators</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @integrators.each do |integrator| %>
      <tr>
        <td><%# link_to 'Show', integrator %></td>
        <td><%# link_to 'Edit', edit_integrator_path(integrator) %></td>
        <td><%# link_to 'Destroy', integrator, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%# end %>
  </tbody>
</table>

<br>

<%= link_to 'New Integrator', new_integrator_path %>
